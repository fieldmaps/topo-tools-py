[project]
name = "edge-extender"
version = "0.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["duckdb", "psutil"]

[dependency-groups]
dev = ["pre-commit", "ruff"]

[tool.ruff]
fix = true

[tool.ruff.lint]
select = ["ALL"]
ignore = ["COM812", "D203", "D213", "S603", "S607", "S608"]
